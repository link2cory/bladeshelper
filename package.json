{
  "name": "bladeshelper",
  "version": "0.0.1",
  "description": "Blades in the Dark Helper",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "version": "lerna version -a",
    "changed": "lerna changed -a",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.21.0"
  }
}
